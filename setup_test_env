#!/bin/sh
cd "$TEST_DIR/$PROJECT"
if [ "$1" = "" ]; then
    branch="master"
else
    branch=$1
fi
git clean -f -d
git checkout master
git pull
git checkout "$branch"
git clean -f -d
git pull
cd build/jenkins
for F in $(cat $TEST_DIR/jenkins_scripts); do
    git checkout master $F
done
